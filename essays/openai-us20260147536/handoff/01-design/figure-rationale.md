# Figure Rationale

## Text-only run

No figure assets exist for this run (`input/figures/` is empty; text-only upload, image fetch blocked by network policy). No figures are selected, so there are no per-figure purpose/effect blocks. This file exists to satisfy the Phase 1 output contract and to record the compensations Phase 2 must make.

## What the essay loses without figures, and the prose compensation

- **FIG. 1 / FIG. 2 (compute engine and CIM macro block diagrams)** would have established the data path (input buffer → CIM macro → dequantization → output) at a glance. Compensation: §2 opens with a two-sentence verbal map of the pipeline before any component detail (one new concept per paragraph, per reader-profile rule 5).
- **FIG. 4 / FIGS. 34-39 (mantissa alignment functional block and shift sequence)** depict the claimed core step and would have been the cover candidate. Compensation: §2 walks the alignment with one worked-scale analogy (lining up numbers by decimal point before adding, then remembering where the point was), anchored to `[0104]` and `[0199]`; the essay's comparison is clearly the essay's, the patent's values stay verbatim.
- **FIGS. 43-48 (double-bitcell designs)** would have made the two-banks-one-bitline idea visible. Compensation: §4 describes the two bitcells per memory cell verbally via `[0012]`/`[0015]`/`[0154]` spans, without part-number density (no more than the wgt_sel signal named in prose).
- **FIG. 42 / FIG. 44 / FIG. 50 (timing diagrams)** are silicon-depth evidence rather than reader-payload; their existence (not their content) is cited in §5 as depth evidence.

## Caption and surface consequences

- No cover caption exists, so SURF-003 (cover caption numeral budget) is moot; the title and first two lines of ¶1 carry the entire feed card (title-lead-candidates.md is written against this).
- gate_figure_use / FIGUSE-001 expectations: zero selected figures is the declared, correct state for this run; goal-2 (figure + specification sufficiency) is served on the specification side by the spec-dense Quotable span set (25 anchors across problem, mechanism, effects, and depth evidence).
